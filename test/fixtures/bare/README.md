# acme-bare

A small library. Nothing here answers any obligation, which is the point: this
fixture is the floor against which every absence rule is measured.

## Usage

```js
import { run } from 'acme-bare';
run();
```
